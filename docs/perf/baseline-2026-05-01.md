# Performance baseline — 2026-05-01

**Purpose:** Anchor numbers before iterative CRM performance work (proxy/auth, dashboard aggregates, RLS planner wrap, region alignment). Re-measure **24 h after each production deploy** that touches auth, data fetching, or database policies.

## Vercel Speed Insights (manual export)

Values below are **approximate** from the pre-change dashboard snapshot referenced in the CRM performance plan (protected routes showed multi-second server time).

| Route / URL | TTFB (approx.) | Notes |
| ----------- | -------------- | ----- |
| `/dashboard` | ~3.8 s | Primary pain route; heavy auth + data path |
| `/login` | Poor FCP/LCP in snapshot | Public; proxy should no longer match |
| `/companies` | Not separately captured | Re-record after RPC filter buckets |

**Next steps**

1. Open **Vercel → Project → Speed Insights** and paste fresh **P50/P75/P95** for `/dashboard`, `/companies`, `/login` into a dated subsection after each deploy.
2. Correlate with **Supabase** query duration (if logging enabled) and Postgres **Advisor** runs documented in [`docs/SUPABASE_SCHEMA.md`](../SUPABASE_SCHEMA.md) (*Database linter snapshot*).

## Database

- **RLS planner wrap:** applied 2026-05-01 (see `rls_planner_subselect_wrap_*` migrations or [`src/sql/rls-planner-subselect-wrap.sql`](../../src/sql/rls-planner-subselect-wrap.sql)).
- **RPC hot paths:** run [`docs/perf/hot-paths-explain.md`](hot-paths-explain.md) templates on staging after substantive DB changes.
